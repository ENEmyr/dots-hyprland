[Unit]
Description=SSH tunnel to the Ollama instance on the remote Ollama host
After=network-online.target

[Service]
Type=simple
ExecStart=/usr/bin/ssh -N -o BatchMode=yes -o ExitOnForwardFailure=yes -o ServerAliveInterval=30 -o ServerAliveCountMax=3 -L 127.0.0.1:11435:127.0.0.1:11434 OLLAMA_SSH_HOST
Restart=always
RestartSec=5

[Install]
WantedBy=default.target
